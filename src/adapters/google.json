{
  "id": "google",
  "hosts": [
    "google.com",
    "google.nl",
    "google.de",
    "google.co.uk",
    "google.fr",
    "google.be",
    "google.at",
    "google.ch",
    "google.es",
    "google.it",
    "google.pt",
    "google.ie",
    "google.se",
    "google.fi",
    "google.dk",
    "google.no",
    "google.pl",
    "google.ca",
    "google.com.au"
  ],
  "unitSelector": "#tads, #tadsb, #bottomads, [data-dsktp-pla], #rso [data-text-ad], #rso .MjjYud",
  "labelSelectors": [],
  "adSelectors": [
    "#tads",
    "#tadsb",
    "#bottomads",
    "[data-text-ad]",
    "[data-dsktp-pla]",
    "a[id^=\"plap_\"]"
  ],
  "adContainerSelector": "#tads, #tadsb, #bottomads, [data-dsktp-pla]",
  "feedRootSelector": "#rcnt",
  "verified": "2026-09-24 live DOM, queries \"car insurance\" and \"running shoes\", All and Shopping (udm=28) tabs. Ad blocks sit in #tads (under #tvcap), [data-dsktp-pla] (under #cnt, or as a data-dsktp-pla=false wrapper inside #rso with its own \"Sponsored results\" header), and #bottomads > #tadsb. On the Shopping tab the \"Sponsored products\" carousel is #tads holding [data-pla] items and no [data-text-ad], so #tads itself is the marker. These containers hold only ads, so adContainerSelector hides the outermost one and its header goes with it. Per-ad text carries no Sponsored label, so detection is structural. #rso .MjjYud nests (one wrapper held 11 results): the scanner keeps innermost units only. [data-pla] is deliberately not a marker on its own: only seen inside #tads, and an organic product module using it would be hidden whole. Selector avoids complex :not() (happy-dom ignores it). Re-checked 2026-09-24 (evening) after a live report that the top \"Sponsored products\" carousel stayed visible: on \"shoes\" (English UI) and \"schoenen\" (Dutch UI) that carousel is div.vbIt3d[data-dsktp-pla=true] under #cnt > div.YNk70c > div.SLPe5b, OUTSIDE #rcnt, holding a[id^=plap_] links through /aclk and no [data-text-ad]; it matches unitSelector, adSelectors and adContainerSelector, the scanner scans the whole document (feedRootSelector is informational only), and Google's scripts do not restore an inline display:none on it (observed 6 s, no attribute mutations). The synthetic fixture google-ad-containers.html carries that exact skeleton, and google-search.html has had the same block since session 3; both pass. So the miss is not in the adapter: check that the running build is current. Later that evening: the carousel is NOT in the document a page-context fetch() of the same URL returns (1.5 MB, #tads present, no data-dsktp-pla, twice), yet it is in the DOM by readyState complete and arrives whole with its attribute; so Google renders it from the streamed page, before document_idle, and the initial full scan sees it. The mutation path was exercised anyway: tests/e2e/google-late.spec.ts injects the carousel after load, as a whole subtree and as innerHTML into an existing wrapper, and both are hidden by the built extension. The one shape that would be missed, a marker attribute set on a node already in the page, is not what Google does (test.fixme on record). Sifter is loaded only in the pnpm dev profiles on this machine, not in the regular Chrome or Edge, so the open question is which browser the report came from. Hotel ads on \"hotels amsterdam\" are [data-text-ad] inside a data-dsktp-pla=false wrapper (covered). Local pack on \"shoe store amsterdam\" and \"plumber amsterdam\" carried no Sponsored/ad marker that day, so promoted places are unverified; Google Maps (google.com/maps) is a different app with its own markup and is NOT covered. Ads are not served on every request: an empty #tads is normal and hiding it is harmless."
}
