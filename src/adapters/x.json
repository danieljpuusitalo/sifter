{
  "id": "x",
  "hosts": ["x.com", "twitter.com"],
  "feedRootSelector": "[data-testid=\"primaryColumn\"]",
  "unitSelector": "[data-testid=\"cellInnerDiv\"] > div",
  "labelSelectors": [],
  "adSelectors": ["[data-testid=\"placementTracking\"] > article[data-testid=\"tweet\"]"],
  "blocks": [{ "selector": "aside[aria-label=\"Who to follow\"]", "category": "suggested" }],
  "verified": "2026-09-24 live DOM, home timeline: a promoted tweet is the only article whose direct parent is [data-testid=placementTracking]. Organic tweets with video also contain a placementTracking node, but inside the article, never as its parent. The visible 'Ad' text is not used. Units are the cell's child, not the virtualised cell itself, so X's list keeps its measured positions. Daniel confirmed promoted posts are hidden on his logged-in timeline the same day (few ads on X, but the ones that appeared went), then doubted one hidden post was an ad at all. Re-checked the same evening over 41 tweets of his For You timeline: the structural marker and X's own visible 'Ad' label agreed on exactly 8 posts with no disagreement either way, and none of the 8 carried a timestamp (organic tweets all do). X ads read like ordinary posts from small accounts, which is why a hidden one looks random. Precision holds. 2026-09-25: with another ad blocker hiding placementTracking, Sifter skips the unit (foreign-hidden) instead of stacking a placeholder on it."
}
